apiVersion: v1
kind: Service
metadata:
  labels:
    name: scaletestapp
  name: scaletestapp
spec:
  selector:
    app: scaletestapp
  ports:
    - protocol: TCP
      name: http
      port: 18080
      targetPort: 8080
  type: LoadBalancer